# Definimos un número a evaluar
numero = 7

# Usamos el operador módulo % para verificar si es par
if numero % 2 == 0:
    print(f"El número {numero} es PAR (resto 0)")
else:
    print(f"El número {numero} es IMPAR (resto distinto de 0)")